import 'package:flutter/material.dart';


class BuilderLayout extends StatelessWidget {
  const BuilderLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}